name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-disabled: true

      - name: Build APK
        run: |
          # Agar AttendBot folder ke andar project hai
          if [ -f "AttendBot/build.gradle" ]; then cd AttendBot; fi
          
          # Permission aur Build
          chmod +x gradlew || true
          gradle assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AttendBot-APK
          path: "**/build/outputs/apk/debug/*.apk"
